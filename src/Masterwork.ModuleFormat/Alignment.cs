namespace Masterwork.ModuleFormat;

/// <summary>
/// Horizontal text alignment for a <see cref="TextNode"/> or <see cref="ImageNode"/>, per the
/// closed set defined by the MWS format spec.
/// </summary>
public enum Alignment
{
    Left,
    Center,
    Right,
    Justified,
}
